[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = []
  included_files = ["netlify/functions/**"]

[functions."plantnet-identify"]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"
